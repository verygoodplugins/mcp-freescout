# FreeScout API Configuration
FREESCOUT_URL=https://your-freescout-domain.com
FREESCOUT_API_KEY=your-api-key-here

# Optional: Default user ID for assignments (default: 1)
FREESCOUT_DEFAULT_USER_ID=1

# Optional: GitHub Configuration (for PR creation)
GITHUB_TOKEN=your-github-token
GITHUB_REPO=owner/repo

# Optional: Working directory for code operations
# If not specified, uses the current working directory
# WORKING_DIRECTORY=/path/to/project